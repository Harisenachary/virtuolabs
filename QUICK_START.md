# 🚀 Quick Start Guide

## ⚠️ IMPORTANT: Node.js Required

This React website **requires Node.js** to run. It cannot run without it.

## 📥 Install Node.js (Takes 2 minutes)

1. **Download:** https://nodejs.org/ (Click the LTS version - green button)
2. **Install:** Run the installer, use all default settings
3. **Restart:** Close and reopen PowerShell/Command Prompt

## ✅ Verify Installation

Open PowerShell and run:
```powershell
node --version
npm --version
```

You should see version numbers like `v18.x.x` and `9.x.x`

## 🎯 Run the Website

Once Node.js is installed:

```powershell
# Navigate to project folder (if not already there)
cd C:\Users\Aditya\OneDrive\Desktop\virtuolabs

# Install dependencies (first time only)
npm install

# Start the website
npm run dev
```

## 🌐 Access the Website

After running `npm run dev`, you'll see:
```
➜  Local:   http://localhost:8080/
```

**Open this URL in your browser:** http://localhost:8080

## 🛑 Stop the Server

Press `Ctrl + C` in the terminal

---

**The website is ready to run once Node.js is installed!**

